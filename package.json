{
  "name": "mb-server",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build:client": "npm --prefix mb-client run build",
    "app": "npm run build:client && node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "path": "^0.12.7"
  }
}
